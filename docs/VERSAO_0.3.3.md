# Smart Finance 0.3.3

- Corrigido o fluxo do GitHub Actions que falhava ao preparar Node.js ou Java.
- Removido o cache prematuro de npm e Gradle antes da criação dos arquivos necessários.
- Node.js ajustado para a versão 22.
- Compatibilidade com ramificações `main` e `principal`.
- Corrigida a inserção de permissões no AndroidManifest.xml.
- Cada compilação passa a receber um `versionCode` crescente para permitir atualizações do APK.
